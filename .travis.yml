sudo: true
language: python
env:
  EXCELCY_TEMP_PATH=${TRAVIS_BUILD_DIR}
python:
  - 3.6
install:
 - pip install -r requirements.txt
 - pip install -r requirements-test.txt
script:
 - spacy download en
 - tox
after_success:
 - coveralls
